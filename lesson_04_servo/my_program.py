from helper import move_to, pause, prepare_servo

SERVO_PIN = None


def my_program():
    # Step 1 — pick your servo signal pin (use SERVO_PIN above; must be 4, 5, 6, 17, or 27)
    # prepare_servo(SERVO_PIN)
    #
    # Step 2 — left, middle, right — pause between moves so the servo can finish
    # move_to(0)
    # pause(1)
    # move_to(90)
    # pause(1)
    # move_to(180)
    pass
