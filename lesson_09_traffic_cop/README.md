# Lesson 9 — Traffic cop (Groq)

**Edit only:** `my_program.py`

Classify sentences as **CONTROL** (hardware) vs **GENERAL** (chat). Optional LED blink on CONTROL.

## Groq key

Same as Lesson 8 — `GROQ_API_KEY` in `.env`.

## Run

```bash
cd ~/lessons
./run 9
./reset 9
```
