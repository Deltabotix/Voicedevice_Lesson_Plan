from helper import fade_brightness, pause, prepare_strip, set_brightness

MAX_BRIGHTNESS = 60


def my_program():
    # Step 1 — prepare the 12 V strip (MOSFET on pin 12; adapter powers the strip)
    # prepare_strip()
    #
    # Step 2 — fade up to MAX_BRIGHTNESS, pause, fade down, end at 0%
    # set_brightness(0)
    # fade_brightness(0, MAX_BRIGHTNESS, step=15, pause_ms=120)
    # pause(1)
    # fade_brightness(MAX_BRIGHTNESS, 0, step=15, pause_ms=120)
    # set_brightness(0)
    pass
