from helper import beep, pause_ms, prepare_buzzer

BUZZER_PIN = None
BUZZER_FREQ_HZ = 2500


def my_program():
    # Step 1 — get the buzzer pin ready (use BUZZER_PIN above; kit default is 22)
    # prepare_buzzer(BUZZER_PIN)
    #
    # Step 2 — short — short — long (use BUZZER_FREQ_HZ for pitch)
    # beep(150, BUZZER_FREQ_HZ)
    # pause_ms(80)
    # beep(150, BUZZER_FREQ_HZ)
    # pause_ms(80)
    # beep(400, BUZZER_FREQ_HZ)
    pass
