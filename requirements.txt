# Lessons use the kit Python at /opt/voicedevice/voicekit_v2/venv
# (RPi.GPIO, groq, python-dotenv, numpy, tflite-runtime, … — see voicekit_v2/full_requirement.txt).
#
# Image build — install only extras not already in voicekit venv:
#   ./image_setup_venv.sh

sounddevice>=0.4.6
