# Lesson 6 — Speaker

**Edit only:** `my_program.py`

Play a test WAV through the kit speaker (`aplay`).

## Run

```bash
cd ~/lessons
./run 6
./reset.sh 6
```

Add `test.wav` to this folder if the Pi has no system demo sounds.
