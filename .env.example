# Copy to .env in this folder (or use /home/voicedevice/.env on the kit image):
#   cp .env.example .env
#
# Get a free key at https://console.groq.com — needed for lessons 6 and 7 only.
GROQ_API_KEY=
