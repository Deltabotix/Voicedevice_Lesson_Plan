from helper import CAPTURE_FILE, check_audio_tools, loudness_level, play_sound, record

INPUT_DEVICE = ""
OUTPUT_DEVICE = ""
RECORD_SECONDS = 3


def my_program():
    # Step 1 — record, show loudness, play back (keep speaker volume low)
    # check_audio_tools()
    # record(RECORD_SECONDS, CAPTURE_FILE, INPUT_DEVICE)
    # loudness_level(CAPTURE_FILE)
    # play_sound(CAPTURE_FILE, OUTPUT_DEVICE)
    pass
